# Core LangChain and NVIDIA AI
# langchain-core>=1.0.0 is required because ModelProfile was introduced in 1.0.0.
# Older langchain-core (0.3.x) does not export ModelProfile from
# langchain_core.language_models, causing an ImportError at runtime.
langchain>=1.0.0
langchain-nvidia-ai-endpoints>=1.0.0
langchain-core>=1.0.0
langchain-community>=0.3.0

# Web framework for web interface
flask>=2.0.0
flask-cors>=6.0.0

# Environment management
python-dotenv>=1.0.0

# RAG dependencies for web search
duckduckgo-search>=6.0.0
beautifulsoup4>=4.12.0
requests>=2.31.0

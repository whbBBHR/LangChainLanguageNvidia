# Core LangChain and NVIDIA AI
# Note: langchain-nvidia-ai-endpoints>=1.2.0 imports ModelProfile from langchain_core,
# which was added in langchain-core>=1.2.5. Pinning langchain-core>=1.2.5 ensures
# all packages are installed with compatible versions.
langchain>=0.3.0
langchain-nvidia-ai-endpoints>=1.2.0
langchain-core>=1.2.5
langchain-community>=0.3.0

# Web framework for web interface
flask>=2.0.0
flask-cors>=6.0.0

# Environment management
python-dotenv>=1.0.0

# RAG dependencies for web search
duckduckgo-search>=6.0.0
beautifulsoup4>=4.12.0
requests>=2.31.0
